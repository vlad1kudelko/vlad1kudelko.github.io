name: ci
on:
  push:
    branches:
      - "main"
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: vlad1kudelko/sitegen27
      env:
        DOMAIN: 'vlad1kudelko.gitlab.io'
    steps:
      - run: |
          cd tailwind && bash build.sh
          node /app/build.js
